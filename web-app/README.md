# web-app

This folder contains the static landing page for `shelby-mcp`.

Deployment target:

- `https://shelby-mcp.okwn.cc`

Files:

- `index.html`
- `styles.css`

The site is intentionally framework-free so it can be deployed as a simple static asset bundle without affecting the main MCP server repository structure.
